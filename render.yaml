services:
  - type: web
    name: theo-ai-site
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: theo-ai-database
          property: connectionString
